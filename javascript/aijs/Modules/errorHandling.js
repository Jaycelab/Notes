/*querySelector only returns the first instance of an element , however 
querySelectorAll returns entire instances of an element

In most cases, ALL is used for menu bars or image lots etc,
For more simple and quick processes, querySelector is suggested
querySelector and ID is preferred as targeting classes can create duplicates
due ot the naming convention 
export const randomName = "Random Name";

QS can both be used to extract an ID or class by wrapping in "" and adding
in either a . or #

parentNode method extracts the parent element of the children element. Eg, 
heading.parentNode

childNodes extras allt the children elements of the parent element
any line breaks and type of element will be returned when accessing a child node

nextElementSibling can be used to access different children elements WITHIN a
parent element, and not the children element. Example is having a h1, p within a container
parent element 



const errorHandlingFunction = () => {
    console.log("I am error handling function");
};

export default errorHandlingFunction;

// can't do this
// const sayHi = () => {
//     console.log("Hello from usersignup.js file");
// };

// export default sayHi;
export const randomName = "Random Name";

const errorHandlingFunction = () => {
    console.log("I am error handling function");
};

export default errorHandlingFunction;

// can't do this
// const sayHi = () => {
//     console.log("Hello from usersignup.js file");
// };

// export default sayHi;
export const randomName = "Random Name";

const errorHandlingFunction = () => {
    console.log("I am error handling function");
};

export default errorHandlingFunction;

// can't do this
// const sayHi = () => {
//     console.log("Hello from usersignup.js file");
// };

// export default sayHi;
export const randomName = "Random Name";

const errorHandlingFunction = () => {
    console.log("I am error handling function");
};

export default errorHandlingFunction;

// can't do this
// const sayHi = () => {
//     console.log("Hello from usersignup.js file");
// };

// export default sayHi;
export const randomName = "Random Name";

const errorHandlingFunction = () => {
    console.log("I am error handling function");
};

export default errorHandlingFunction;

// can't do this
// const sayHi = () => {
//     console.log("Hello from usersignup.js file");
// };

// export default sayHi;
export const randomName = "Random Name";

const errorHandlingFunction = () => {
    console.log("I am error handling function");
};

export default errorHandlingFunction;

// can't do this
// const sayHi = () => {
//     console.log("Hello from usersignup.js file");
// };

// export default sayHi;
*/

